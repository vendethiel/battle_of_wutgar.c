#pragma once

void attack_enemy_chimaera(t_enemy*, t_chim*);

int attack_enemy_bite(t_char*, t_enemy*);
int attack_enemy_earthquake(t_char*, t_enemy*);
int attack_enemy_doom(t_char*, t_enemy*);

// boss
int attack_enemy_death(t_char*, t_enemy*);
